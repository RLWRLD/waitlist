{
  "name": "rldx-waitlist-qa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:option": "npx playwright test option-edge-cases",
    "test:flow": "npx playwright test flow-edge-cases",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
